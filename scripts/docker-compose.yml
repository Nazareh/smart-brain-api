version: '3.1'

services:

  db:
    image: postgres:latest
    restart: always
    ports:
      - 5432:5432
    environment:
      POSTGRES_DB: smart-brain-db
      POSTGRES_PASSWORD: supersecret
      POSTGRES_USER: admin
